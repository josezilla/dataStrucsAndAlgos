# python program performs a quicksort

#import random

#arrayToSort = []
#for x in range(10):
#  arrayToSort.insert(0, random.randint(1,101))
 

arrayToSort = [5, 14, 86, 14, 25, 99, 55, 57, 85, 17]

sortedArray = []
i = -1
rightMark = 0
for x in arrayToSort:
	if x >= 55:
		sortedArray.insert(i, x)
		rightMark = rightMark -- 
print rightMark

# use a variable for the key index
# use a variable to represent the value of the key
# use a variable to represent the pivot value

pivot = 5

